<!-- Navigation Bar -->
{# <div class="w3-bar w3-white w3-large"> 
	<a href="{{ path('app_index' )}} " class="w3-bar-item w3-button w3-red w3-mobile">
		<i class="fa fa-bed w3-margin-right"></i>LOGO</a>
	{% if template_var is not defined %}

		<a href="{{ path('index_template') }} " class="w3-bar-item w3-button w3-mobile">NOS ANNONCES</a>

	{% else %}

		<a href="{{ path('index_template') }} " class="w3-bar-item w3-button w3-mobile">NOS ANNONCES</a>

	{% endif %}
	<a href="#about" class="w3-bar-item w3-button w3-mobile">A PROPOS</a>
	<a href="#contact" class="w3-bar-item w3-button w3-mobile">CONTACT</a>
	<a href="#contact" class="w3-bar-item w3-button w3-right w3-light-grey w3-mobile">Réserver Maintenant</a>

</div>
<!-- Header -->
<header class="w3-display-container w3-content" style="max-width:1350px;">
	<img class="w3-image" src="{{ asset('assets/img/piscine4.jpg') }} " alt="piscine" style="min-width:900px" width="100%" height="200px">
	
</header>#}
    <!-- Navbar -->
    <div class="w3-top">
    <div class="w3-bar w3-black w3-card">
        <a class="w3-bar-item w3-button w3-padding-large w3-hide-medium w3-hide-large w3-right" href="javascript:void(0)" onclick="myFunction()" title="Toggle Navigation Menu"><i class="fa fa-bars"></i></a>
        <a href="{{ path('app_index') }}" class="w3-bar-item w3-button w3-padding-large">HOME</a>
        
            <div class="w3-dropdown-hover w3-hide-small">
             <a href="" class="w3-bar-item w3-button w3-mobile">CATEGORIES</a> 
                <div class="w3-dropdown-content w3-bar-block w3-card-4">

                    {# <a href="{{ path('index_category', {'categoryName' : category.name}) }}" class="w3-bar-item w3-button">{{ category.name }}</a> #}
        
                </div>
            </div>
        <a href="#contact" class="w3-bar-item w3-button w3-padding-large w3-hide-small">CONTACT</a>
        
            <div class="w3-dropdown-hover w3-hide-small">
               <button class="w3-padding-large w3-button" title="Options">OPTIONS <i class="fa fa-caret-down"></i></button>     
               <div class="w3-dropdown-content w3-bar-block w3-card-4">
                <a href="{{ path('app_index' )}} " class="w3-bar-item w3-button w3-red w3-mobile">
	  	         <i class="fa fa-bed w3-margin-right"></i>NOS LOCATIONS</a>
	             {% if template_var is not defined %}

		        <a href="{{ path('index_template') }} " class="w3-bar-item w3-button w3-mobile">NOS ANNONCES</a>

            	{% else %}

		        <a href="{{ path('index_template') }} " class="w3-bar-item w3-button w3-mobile">NOS ANNONCES</a>

	            {% endif %}

               </div>
            </div>
      
    </div>
    </div>

    <!-- Navbar on small screens (remove the onclick attribute if you want the navbar to always show on top of the content when clicking on the links) -->
    <div id="navDemo" class="w3-bar-block w3-black w3-hide w3-hide-large w3-hide-medium w3-top" style="margin-top:46px">
    {# {% if categories is defined %} #}
        {# {% for category in categories %} #}
            {# <a href="{{ path('index_category', {'categoryName' : category.name}) }}" class="w3-bar-item w3-button w3-padding-large">{{ category.name|upper }}</a> #}
        {# {% endfor %} #}
    {# {% endif %} #}
    <a href="#contact" class="w3-bar-item w3-button w3-padding-large" onclick="myFunction()">CONTACT</a>
    </div>
